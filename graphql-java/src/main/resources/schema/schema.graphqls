schema {
    query: Query
}

type Query {
    bookById(id: ID!): Book
}

type Book {
    id: ID!
    title: String!
    pageCount: Int
    author: Author
}

type Author {
    id: ID!
    firstName: String
    lastName: String
}